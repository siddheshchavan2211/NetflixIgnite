import React from "react";

const BG_Name = ({ title, desc }) => {
  return (
    <div>
      <div>
        <h1>{title}</h1>
        <p>{desc}</p>
      </div>
    </div>
  );
};

export default BG_Name;
